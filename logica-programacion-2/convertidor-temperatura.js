
let celciusInput = prompt("Por favor, ingresa la temperatura en C° con decimales:");



function transforToFahr(){
    // La fórmula para convertir grados celsius en Fahrenheit es: °F = (°C × 9/5) + 32. 
    // Multiplica los grados Celsius por 9/5 y luego suma 32.
    return F = (celciusInput * (9/5) + 32);
}

function transToKelvin(){
    //La fórmula para convertir Celsius (°C) en Kelvin (K) es la siguiente: K = °C + 273,15. 
    return K = celciusInput + 273.15;
}